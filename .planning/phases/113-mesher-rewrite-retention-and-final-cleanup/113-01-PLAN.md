---
phase: 113-mesher-rewrite-retention-and-final-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mesher/storage/queries.mpl
autonomous: true
requirements: [REWR-06, REWR-08]

must_haves:
  truths:
    - "delete_expired_events uses Repo.delete_where + Query.where_raw instead of Repo.execute_raw"
    - "get_all_project_retention uses Query.from + Query.select_raw + Repo.all instead of Repo.query_raw"
    - "get_project_storage uses Query.from + Query.where_raw + Query.select_raw + Repo.all instead of Repo.query_raw"
    - "get_project_settings uses Query.from + Query.where_raw + Query.select_raw + Repo.all instead of Repo.query_raw"
    - "update_project_settings has ORM boundary comment documenting JSONB extraction + COALESCE reason"
    - "check_sample_rate has ORM boundary comment documenting random() + scalar subquery reason"
    - "get_expired_partitions and drop_partition are excluded as DDL/catalog operations"
    - "Zero Repo.query_raw/execute_raw calls remain for data queries (DDL/partition and documented ORM boundaries excluded)"
    - "meshc build mesher compiles with zero errors"
  artifacts:
    - path: "mesher/storage/queries.mpl"
      provides: "4 rewritten retention queries + 2 ORM boundary comments + updated header comment"
      contains: "Query.where_raw"
  key_links:
    - from: "mesher/storage/queries.mpl"
      to: "mesher/services/retention.mpl"
      via: "function imports (delete_expired_events, get_all_project_retention, get_expired_partitions, drop_partition)"
      pattern: "from Storage.Queries import"
    - from: "mesher/storage/queries.mpl"
      to: "mesher/api/settings.mpl"
      via: "function imports (get_project_settings, update_project_settings, get_project_storage)"
      pattern: "from Storage.Queries import"
    - from: "mesher/storage/queries.mpl"
      to: "mesher/ingestion/routes.mpl"
      via: "function import (check_sample_rate)"
      pattern: "from Storage.Queries import"
---

<objective>
Rewrite 4 retention/storage query functions in `mesher/storage/queries.mpl` from raw SQL to ORM Query/Repo pipe chains, add ORM boundary documentation to 2 queries that must retain raw SQL, and verify zero Repo.query_raw/execute_raw calls remain for data queries across all Mesher .mpl files.

Purpose: Complete the Mesher ORM rewrite by eliminating all remaining raw SQL from retention/storage data queries (REWR-06) and verify the zero-raw-SQL data query invariant (REWR-08). DDL/partition operations (get_expired_partitions, drop_partition) and schema.mpl are excluded per success criteria.

Output: 4 rewritten functions + 2 boundary-documented functions + compilation verification + raw SQL audit.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@mesher/storage/queries.mpl
@mesher/services/retention.mpl
@mesher/api/settings.mpl
@mesher/ingestion/routes.mpl
@.planning/phases/112-mesher-rewrite-search-dashboard-and-alerts/112-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite 4 retention/storage queries to ORM and document 2 boundaries</name>
  <files>mesher/storage/queries.mpl</files>
  <action>
Rewrite these 4 functions in the "Retention and storage queries (Phase 93)" section of queries.mpl:

**1. delete_expired_events (line ~804)**
Replace:
```
Repo.execute_raw(pool, "DELETE FROM events WHERE project_id = $1::uuid AND received_at < now() - ($2 || ' days')::interval", [project_id, retention_days_str])
```
With ORM:
```
let q = Query.from(Event.__table__())
  |> Query.where_raw("project_id = ?::uuid AND received_at < now() - (? || ' days')::interval", [project_id, retention_days_str])
Repo.delete_where(pool, Event.__table__(), q)
```
Keep same return type `Int!String`. Add comment: `# Uses ORM Repo.delete_where + Query.where_raw for interval expression instead of Repo.execute_raw.`

**2. get_all_project_retention (line ~823)**
Replace:
```
let rows = Repo.query_raw(pool, "SELECT id::text, retention_days::text FROM projects", [])?
Ok(rows)
```
With ORM:
```
let q = Query.from(Project.__table__())
  |> Query.select_raw(["id::text", "retention_days::text"])
Repo.all(pool, q)
```
Keep same return type `List<Map<String, String>>!String`. Add comment: `# Uses ORM Query.from + Query.select_raw + Repo.all instead of Repo.query_raw.`

**3. get_project_storage (line ~830)**
Replace:
```
let sql = "SELECT count(*)::text AS event_count, (count(*) * 1024)::text AS estimated_bytes FROM events WHERE project_id = $1::uuid"
let rows = Repo.query_raw(pool, sql, [project_id])?
Ok(rows)
```
With ORM:
```
let q = Query.from(Event.__table__())
  |> Query.where_raw("project_id = ?::uuid", [project_id])
  |> Query.select_raw(["count(*)::text AS event_count", "(count(*) * 1024)::text AS estimated_bytes"])
Repo.all(pool, q)
```
Keep same return type `List<Map<String, String>>!String`. Add comment: `# Uses ORM Query.from + Query.where_raw + Query.select_raw + Repo.all instead of Repo.query_raw.`

**4. get_project_settings (line ~843)**
Replace:
```
let rows = Repo.query_raw(pool, "SELECT retention_days::text, sample_rate::text FROM projects WHERE id = $1::uuid", [project_id])?
Ok(rows)
```
With ORM:
```
let q = Query.from(Project.__table__())
  |> Query.where_raw("id = ?::uuid", [project_id])
  |> Query.select_raw(["retention_days::text", "sample_rate::text"])
Repo.all(pool, q)
```
Keep same return type `List<Map<String, String>>!String`. Add comment: `# Uses ORM Query.from + Query.where_raw + Query.select_raw + Repo.all instead of Repo.query_raw.`

**Add ORM boundary comments to these 2 functions (DO NOT change their implementation):**

**5. update_project_settings (line ~838)**
Add multi-line comment before the function explaining: ORM boundary: SET clause uses COALESCE with server-side JSONB extraction ($2::jsonb->>'field')::type to conditionally update only provided fields, falling back to current column value. Repo.update_where takes Map<String,String> which cannot express COALESCE fallback to current column value or server-side JSONB extraction. Intentional raw SQL.

**6. check_sample_rate (line ~851)**
Add multi-line comment before the function explaining: ORM boundary: SELECT random() < COALESCE((SELECT ...), 1.0) uses a server-side random() function comparison with a scalar subquery and COALESCE default. Not expressible via ORM query builder. Intentional raw SQL.

**Also add DDL exclusion comments to these 2 functions (DO NOT change their implementation):**

**7. get_expired_partitions (line ~810)**
Update existing comment to note: DDL/catalog query -- queries pg_inherits/pg_class system catalogs. Excluded from data query raw SQL count per ORM rewrite scope.

**8. drop_partition (line ~818)**
Update existing comment to note: DDL operation (DROP TABLE) -- excluded from data query raw SQL count per ORM rewrite scope.

**Update the file header comment** (line 1-5) to reflect that all data queries now use ORM:
Change "and Repo.query_raw/Repo.execute_raw for complex analytics, PG functions," to something like "with documented ORM boundaries for complex expressions (PG crypto, JSONB extraction, server-side functions)."

**CRITICAL: Preserve all function signatures exactly.** All callers (retention.mpl, settings.mpl, ingestion/routes.mpl) must continue to work without changes.
  </action>
  <verify>
Search queries.mpl for the 4 rewritten functions and confirm they use Query.from pipe chains instead of Repo.query_raw/Repo.execute_raw. Verify the 2 boundary comments and 2 DDL exclusion comments are present.
  </verify>
  <done>
4 retention/storage data query functions use ORM APIs. 2 functions have ORM boundary documentation. 2 DDL functions have exclusion comments. File header updated. All function signatures unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compile Mesher and audit remaining raw SQL calls</name>
  <files>mesher/storage/queries.mpl</files>
  <action>
**Step 1: Compile Mesher.**
Run `meshc build mesher` and verify zero compilation errors. If errors occur, fix them in queries.mpl following established patterns (parse errors, type mismatches, etc.).

**Step 2: Audit remaining raw SQL.**
Search all .mpl files under mesher/ for `Repo.query_raw` and `Repo.execute_raw`. Categorize every remaining call:

Expected remaining raw SQL calls by category:

**A. Two-step PG crypto/function pattern (4 calls in queries.mpl):**
- create_api_key: Repo.query_raw for gen_random_bytes (step 1 of 2)
- revoke_api_key: Repo.query_raw for now() timestamp (step 1 of 2)
- create_user: Repo.query_raw for crypt/gen_salt (step 1 of 2)
- create_session: Repo.query_raw for gen_random_bytes (step 1 of 2)

**B. Documented ORM boundaries in queries.mpl (~17 calls):**
- upsert_issue: arithmetic SET + CASE expression
- assign_issue (unassign branch): NULL value
- check_volume_spikes: nested subquery + HAVING + GREATEST
- extract_event_fields: CASE/jsonb_array_elements/string_agg
- list_issues_filtered (2 branches): conditional WHERE with keyset pagination
- search_events_fulltext: ts_rank with bound parameter in SELECT
- event_breakdown_by_tag: tags->>$2 bound parameter in SELECT
- project_health_summary: three cross-table scalar subqueries
- get_event_neighbors: two scalar subqueries with opposing sorts
- create_alert_rule: INSERT...SELECT JSONB extraction
- evaluate_threshold_rule: cross-join derived tables
- fire_alert (2 calls): jsonb_build_object INSERT + now() UPDATE
- acknowledge_alert: SET column = now()
- resolve_fired_alert: SET column = now()
- update_project_settings: COALESCE + JSONB extraction (NEW boundary)
- check_sample_rate: random() + scalar subquery (NEW boundary)

**C. DDL/partition operations (3 calls):**
- get_expired_partitions: pg_inherits catalog query (queries.mpl)
- drop_partition: DROP TABLE (queries.mpl)
- schema.mpl create_partition: CREATE TABLE PARTITION (2 calls)

**D. Event writer (1 call in writer.mpl):**
- insert_event: INSERT...SELECT JSONB extraction

Verify that categories A, B, C, D account for ALL remaining raw SQL calls. Confirm zero unaccounted-for data queries remain.

**Step 3: Update the section header comment** at line ~800 to reflect completion:
"# --- Retention and storage queries (Phase 93, ORM rewrite Phase 113) ---"
  </action>
  <verify>
Run `meshc build mesher` -- expect zero errors. Run `grep -c 'Repo.query_raw\|Repo.execute_raw' mesher/storage/queries.mpl mesher/storage/writer.mpl mesher/storage/schema.mpl` and verify all calls are accounted for in categories A-D.
  </verify>
  <done>
Mesher compiles with zero errors. All remaining Repo.query_raw/Repo.execute_raw calls are accounted for: 4 two-step PG crypto, ~17 documented ORM boundaries, 3 DDL/partition, 1 event writer. Zero unaccounted data queries remain (REWR-08 satisfied).
  </done>
</task>

</tasks>

<verification>
1. `meshc build mesher` completes with zero errors
2. `delete_expired_events` uses `Repo.delete_where` with `Query.where_raw` (not `Repo.execute_raw`)
3. `get_all_project_retention` uses `Query.from` + `Query.select_raw` + `Repo.all` (not `Repo.query_raw`)
4. `get_project_storage` uses `Query.from` + `Query.where_raw` + `Query.select_raw` + `Repo.all` (not `Repo.query_raw`)
5. `get_project_settings` uses `Query.from` + `Query.where_raw` + `Query.select_raw` + `Repo.all` (not `Repo.query_raw`)
6. `update_project_settings` and `check_sample_rate` have ORM boundary comments
7. `get_expired_partitions` and `drop_partition` have DDL exclusion comments
8. All remaining raw SQL calls across mesher/ are categorized as: PG crypto two-step (4), documented ORM boundaries (~17), DDL/partition (3), or event writer JSONB extraction (1)
9. All function signatures are unchanged -- callers in retention.mpl, settings.mpl, and ingestion/routes.mpl compile without modification
</verification>

<success_criteria>
- REWR-06: 4 of 8 retention/storage queries rewritten to ORM; remaining 2 documented as ORM boundaries, 2 excluded as DDL
- REWR-08: Zero Repo.query_raw/execute_raw calls for data queries remain (all remaining calls are PG crypto two-step, documented ORM boundaries, DDL, or event writer JSONB)
- Mesher compiles with zero errors
- All callers unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/113-mesher-rewrite-retention-and-final-cleanup/113-01-SUMMARY.md`
</output>
