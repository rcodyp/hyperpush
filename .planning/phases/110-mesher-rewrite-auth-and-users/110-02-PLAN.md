---
phase: 110-mesher-rewrite-auth-and-users
plan: 02
type: execute
wave: 2
depends_on: [110-01]
files_modified:
  - mesher/storage/queries.mpl
autonomous: true
requirements: [REWR-01]

must_haves:
  truths:
    - "get_project_by_api_key uses Query.join + Query.where + Query.where_null instead of Repo.query_raw"
    - "get_project_id_by_key uses Query.join + Query.where + Query.where_null instead of Repo.query_raw"
    - "create_api_key uses two-step pattern: Repo.query_raw for gen_random_bytes + Repo.insert for the data insert"
    - "revoke_api_key uses Repo.update_where with Query.where_raw instead of Repo.execute_raw"
    - "All 4 functions compile without errors and maintain existing signatures"
    - "Zero Repo.execute_raw calls remain in the auth/user/session/API-key sections of queries.mpl"
  artifacts:
    - path: "mesher/storage/queries.mpl"
      provides: "Rewritten API key query functions"
      contains: "Query.join"
  key_links:
    - from: "mesher/storage/queries.mpl"
      to: "mesher/ingestion/auth.mpl"
      via: "function import (get_project_by_api_key)"
      pattern: "from Storage.Queries import get_project_by_api_key"
    - from: "mesher/storage/queries.mpl"
      to: "mesher/api/settings.mpl"
      via: "function imports (create_api_key, revoke_api_key, list_api_keys)"
      pattern: "from Storage.Queries import"
---

<objective>
Rewrite the 4 API key query functions in `mesher/storage/queries.mpl` to use ORM Query/Repo APIs instead of raw SQL strings.

Purpose: Eliminate raw SQL from the API key domain. `get_project_by_api_key` and `get_project_id_by_key` are converted from raw JOIN queries to `Query.join` + `Query.where` + `Query.where_null`. `revoke_api_key` uses `Repo.update_where`. `create_api_key` uses the two-step pattern (gen_random_bytes via PG call + Repo.insert).

Output: 4 rewritten functions with maximum ORM usage, completing the auth/user/session/API-key domain rewrite.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-mesher-rewrite-auth-and-users/110-01-SUMMARY.md
@mesher/storage/queries.mpl
@mesher/ingestion/auth.mpl
@mesher/types/project.mpl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite get_project_by_api_key and get_project_id_by_key with ORM JOIN</name>
  <files>mesher/storage/queries.mpl</files>
  <action>
Rewrite `get_project_by_api_key` (line ~99) and `get_project_id_by_key` (line ~112) to use ORM Query builder with JOIN.

**get_project_by_api_key** — Current: `Repo.query_raw(pool, "SELECT p.id::text, p.org_id::text, p.name, p.platform, p.created_at::text FROM projects p JOIN api_keys ak ON ak.project_id = p.id WHERE ak.key_value = $1 AND ak.revoked_at IS NULL", [key_value])`

Rewrite to:
```
pub fn get_project_by_api_key(pool :: PoolHandle, key_value :: String) -> Project!String do
  let q = Query.from(Project.__table__())
    |> Query.join_as(:inner, ApiKey.__table__(), "ak", "ak.project_id = projects.id")
    |> Query.where_raw("ak.key_value = ?", [key_value])
    |> Query.where_raw("ak.revoked_at IS NULL", [])
    |> Query.select_raw(["projects.id::text", "projects.org_id::text", "projects.name", "projects.platform", "projects.created_at::text"])
  let rows = Repo.all(pool, q)?
  if List.length(rows) > 0 do
    let row = List.head(rows)
    Ok(Project { id: Map.get(row, "id"), org_id: Map.get(row, "org_id"), name: Map.get(row, "name"), platform: Map.get(row, "platform"), created_at: Map.get(row, "created_at") })
  else
    Err("not found")
  end
end
```

CRITICAL NOTES on JOIN syntax:
- `Query.join_as(q, type_atom, table, alias, on_clause)` — the on_clause is a raw SQL string
- The `type_atom` must be `:inner` (not `:left` or other)
- Table names must match the schema (`"api_keys"` from `ApiKey.__table__()`)
- The on-clause references table names directly (no alias needed for projects since it's the FROM table)
- For WHERE conditions on the joined table, use `Query.where_raw` with the alias prefix (`ak.key_value`, `ak.revoked_at`)
- For `IS NULL`, use `Query.where_raw("ak.revoked_at IS NULL", [])` since `Query.where_null` only supports unqualified field atoms

IMPORTANT: Check that `Query.select_raw` with table-qualified columns (e.g., `"projects.id::text"`) generates valid SQL. The SQL builder should emit these verbatim since they have the `RAW:` prefix. If it doesn't work correctly, fall back to unqualified names with appropriate SELECT.

**get_project_id_by_key** — Same JOIN pattern but only select the project ID:
```
pub fn get_project_id_by_key(pool :: PoolHandle, key_value :: String) -> String!String do
  let q = Query.from(Project.__table__())
    |> Query.join_as(:inner, ApiKey.__table__(), "ak", "ak.project_id = projects.id")
    |> Query.where_raw("ak.key_value = ?", [key_value])
    |> Query.where_raw("ak.revoked_at IS NULL", [])
    |> Query.select_raw(["projects.id::text"])
  let rows = Repo.all(pool, q)?
  if List.length(rows) > 0 do
    Ok(Map.get(List.head(rows), "id"))
  else
    Err("not found")
  end
end
```

IMPORTANT: These two functions are called from:
- `mesher/ingestion/auth.mpl` — imports `get_project_by_api_key`
- `mesher/ingestion/pipeline.mpl` — may import `get_project_id_by_key`
Function signatures MUST NOT change.

Also ensure `from Types.Project import Organization, Project, ApiKey` import line at top of queries.mpl already includes `ApiKey` — it should, since it's already there.
  </action>
  <verify>Run `cargo build -p meshc 2>&1 | tail -5` to verify compiler builds. Visually confirm both functions use `Query.join_as` + `Query.where_raw` instead of `Repo.query_raw`. Grep for `Repo.query_raw` in the API key section — should be zero for these two functions.</verify>
  <done>get_project_by_api_key uses Query.join_as + Query.where_raw (zero raw SQL). get_project_id_by_key uses Query.join_as + Query.where_raw (zero raw SQL). Both function signatures unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite create_api_key and revoke_api_key</name>
  <files>mesher/storage/queries.mpl</files>
  <action>
Rewrite `create_api_key` (line ~89) and `revoke_api_key` (line ~122).

**create_api_key** — Current: `Repo.query_raw(pool, "INSERT INTO api_keys (project_id, key_value, label) VALUES ($1::uuid, 'mshr_' || encode(gen_random_bytes(24), 'hex'), $2) RETURNING key_value", [project_id, label])`

Rewrite using two-step pattern:
```
pub fn create_api_key(pool :: PoolHandle, project_id :: String, label :: String) -> String!String do
  # Step 1: Generate API key value via PG (mshr_ prefix + 48 hex chars)
  let key_rows = Repo.query_raw(pool, "SELECT 'mshr_' || encode(gen_random_bytes(24), 'hex') AS key_value", [])?
  if List.length(key_rows) > 0 do
    let key_value = Map.get(List.head(key_rows), "key_value")
    # Step 2: Insert API key with ORM
    let fields = %{"project_id" => project_id, "key_value" => key_value, "label" => label}
    let _ = Repo.insert(pool, ApiKey.__table__(), fields)?
    Ok(key_value)
  else
    Err("create_api_key: key generation failed")
  end
end
```

NOTE: The `Repo.query_raw` remaining is a pure PG function call for random byte generation, not a data query. The actual data INSERT is done via `Repo.insert`.

**revoke_api_key** — Current: `Repo.execute_raw(pool, "UPDATE api_keys SET revoked_at = now() WHERE id = $1::uuid", [key_id])`

Rewrite using two-step pattern for the `now()` timestamp:
```
pub fn revoke_api_key(pool :: PoolHandle, key_id :: String) -> Int!String do
  # Step 1: Get current timestamp from PG
  let ts_rows = Repo.query_raw(pool, "SELECT now()::text AS ts", [])?
  if List.length(ts_rows) > 0 do
    let ts = Map.get(List.head(ts_rows), "ts")
    # Step 2: Update with ORM
    let q = Query.from(ApiKey.__table__())
      |> Query.where_raw("id = ?::uuid", [key_id])
    let _ = Repo.update_where(pool, ApiKey.__table__(), %{"revoked_at" => ts}, q)?
    Ok(1)
  else
    Err("revoke_api_key: timestamp generation failed")
  end
end
```

NOTE: The return type changes from `Int!String` to `Int!String` (same). The `Repo.update_where` returns `Result<Map<String,String>, String>` (the updated row). We discard it and return `Ok(1)` to match the original `Repo.execute_raw` return pattern (affected row count).

IMPORTANT: Check the `Repo.update_where` return type. From the typechecker: `Repo.update_where(PoolHandle, String, Ptr, Ptr) -> Ptr (pool, table, fields_map, query -> Result<Map, String>)`. It returns a Map (the updated row), not an Int. The original function returns `Int!String`. We need to return `Ok(1)` after a successful update, matching the original behavior.

IMPORTANT: Preserve function signatures exactly. Callers in `mesher/api/settings.mpl` import these functions.
  </action>
  <verify>Run `cargo build -p meshc 2>&1 | tail -5` to verify compiler builds. Visually confirm: create_api_key uses Repo.insert for the data INSERT, revoke_api_key uses Repo.update_where. Grep for `Repo.execute_raw` in the API key section — should be zero.</verify>
  <done>create_api_key uses Repo.query_raw for key generation + Repo.insert for data insert. revoke_api_key uses Repo.query_raw for now() timestamp + Repo.update_where for the update. Zero Repo.execute_raw in the API key domain. All function signatures unchanged.</done>
</task>

</tasks>

<verification>
1. All 4 API key functions (create_api_key, get_project_by_api_key, get_project_id_by_key, revoke_api_key) are rewritten
2. `get_project_by_api_key` uses `Query.join_as` + `Query.where_raw` — no Repo.query_raw
3. `get_project_id_by_key` uses `Query.join_as` + `Query.where_raw` — no Repo.query_raw
4. `create_api_key` uses `Repo.insert` for data insert (Repo.query_raw only for key generation)
5. `revoke_api_key` uses `Repo.update_where` (Repo.query_raw only for now() timestamp)
6. Zero `Repo.execute_raw` calls in the entire auth/user/session/API-key domain
7. Function signatures unchanged — all callers (auth.mpl, settings.mpl, pipeline.mpl) work without changes
8. Combined with Plan 01: all 9 auth/user/session/API-key functions are rewritten
9. `cargo build -p meshc` succeeds
</verification>

<success_criteria>
- Zero `Repo.execute_raw` calls in auth/user/session/API-key domain of queries.mpl
- JOIN-based queries use ORM Query.join_as instead of raw SQL JOINs
- Data inserts use Repo.insert; data updates use Repo.update_where
- Repo.query_raw used ONLY for PG expression evaluation (gen_random_bytes, now()), not for data queries
- All 9 auth/user/session/API-key functions (combined with Plan 01) rewritten
</success_criteria>

<output>
After completion, create `.planning/phases/110-mesher-rewrite-auth-and-users/110-02-SUMMARY.md`
</output>
