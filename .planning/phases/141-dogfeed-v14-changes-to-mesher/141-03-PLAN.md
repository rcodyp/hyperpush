---
phase: 141-dogfeed-v14-changes-to-mesher
plan: 03
type: execute
wave: 2
depends_on: [141-01, 141-02]
files_modified: []
autonomous: false
requirements: [DOGFEED-141]

must_haves:
  truths:
    - "meshc build mesher/ succeeds with zero errors after crypto stdlib changes"
    - "meshc test mesher/tests/ runs and reports results for fingerprint and validation tests"
    - "User confirms build passes and tests run (at least some pass)"
  artifacts:
    - path: "mesher/storage/queries.mpl"
      provides: "Compilable source with Crypto.uuid4() calls"
      contains: "Crypto.uuid4()"
    - path: "mesher/tests/fingerprint.test.mpl"
      provides: "Compilable test file"
    - path: "mesher/tests/validation.test.mpl"
      provides: "Compilable test file"
  key_links:
    - from: "meshc build"
      to: "mesher/storage/queries.mpl"
      via: "compiler invocation"
      pattern: "meshc build mesher"
---

<objective>
Verify the Mesher application compiles successfully after v14 dogfeed changes, and run the new test files to confirm the Testing Framework works end-to-end on Mesher's pure functions.

Purpose: Ensure all Phase 141 changes integrate correctly — no regressions in the production Mesher build, and the new test files execute successfully.

Output: Build verification + test execution results. User confirms the changes work.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-dogfeed-v14-changes-to-mesher/141-01-SUMMARY.md
@.planning/phases/141-dogfeed-v14-changes-to-mesher/141-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Mesher and run tests</name>
  <files></files>
  <action>
Run the Mesher build to verify no compilation errors were introduced by the crypto stdlib changes, then run the new test files.

Step 1 — Build Mesher:
```bash
cd /Users/sn0w/Documents/dev/mesh && meshc build mesher/
```
Expected: exits 0, no error lines. If build fails, read the error carefully:
- "undefined function Crypto.uuid4" — the Crypto stdlib registration is missing from the compiler; check if v14 compiler is in the PATH
- "unexpected token" near the slot pipe — verify the |2> syntax in create_session matches the fingerprint.mpl pattern

Step 2 — Run tests (explicit path to avoid also finding main.mpl):
```bash
cd /Users/sn0w/Documents/dev/mesh && meshc test mesher/tests/
```
Expected output format (from Phase 138 test runner):
```
Running 4 tests in fingerprint.test.mpl...
  . uses custom fingerprint when set
  . non-empty fingerprint takes priority over exception
  . falls back to msg: prefix with normalized message
  . lowercases message in fallback
...
N tests passed, 0 failed
```

If "module not found" for Ingestion.Fingerprint or Ingestion.Validation:
- The test runner copies *.test.mpl to a temp dir. Cross-module imports may need the mesher/ root as context.
- Try running from the mesher/ directory: cd /Users/sn0w/Documents/dev/mesh/mesher && meshc test tests/

If some tests fail due to incorrect expected values in assertions, fix the specific assert_eq calls in the test files to match actual output. Do not change the tested functions themselves — only fix the test expectations.

Capture the full output of both commands.
  </action>
  <verify>
    <automated>cd /Users/sn0w/Documents/dev/mesh && meshc build mesher/ 2>&1 | tail -5</automated>
  </verify>
  <done>meshc build mesher/ exits 0; meshc test mesher/tests/ runs and reports test results (even if some fail — the runner itself must execute)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies build and test results</name>
  <files></files>
  <action>Human reviews build output and test results from Task 1 and approves the Phase 141 changes.</action>
  <verify>
    <automated>echo "Human verification required — see how-to-verify below"</automated>
  </verify>
  <done>User types "approved" confirming build success and correct behavior</done>
  <what-built>
    Phase 141 complete changes:
    - mesher/storage/queries.mpl: create_api_key and create_session now use Crypto.uuid4() instead of pgcrypto gen_random_bytes DB round-trips
    - mesher/mesh.toml: new package manifest declaring Mesher as a Mesh package
    - mesher/tests/fingerprint.test.mpl: unit tests for compute_fingerprint pure logic
    - mesher/tests/validation.test.mpl: unit tests for validate_level, validate_payload_size, validate_bulk_count
  </what-built>
  <how-to-verify>
    Review the build and test output from Task 1. Confirm:

    1. meshc build mesher/ exited 0 (check Task 1 output)
    2. meshc test mesher/tests/ ran and showed test results
    3. Verify the key file changes look correct:
       - grep -A5 "fn create_session" /Users/sn0w/Documents/dev/mesh/mesher/storage/queries.mpl — should show Crypto.uuid4() calls
       - cat /Users/sn0w/Documents/dev/mesh/mesher/mesh.toml — should show package manifest
    4. The session tokens should still be 64-char hex strings (two UUIDs with hyphens stripped = 32+32 chars)

    Type "approved" if the build succeeds and changes look correct.
    Describe any build errors or test failures if something needs fixing.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Phase 141 is complete when:
1. meshc build mesher/ passes with zero errors
2. meshc test mesher/tests/ runs and reports results
3. User approves the changes
4. All modified/created files are committed
</verification>

<success_criteria>
- Mesher compiles without errors after crypto stdlib migration
- New test files execute via meshc test
- User confirms the changes look correct
- Phase 141 changes committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/141-dogfeed-v14-changes-to-mesher/141-03-SUMMARY.md`
</output>
