---
phase: 121-mesh-agent-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill/mesh/SKILL.md
  - skill/mesh/skills/syntax/SKILL.md
  - skill/mesh/skills/types/SKILL.md
autonomous: true
requirements:
  - SKILL-01
  - SKILL-02
  - SKILL-04

must_haves:
  truths:
    - "Root SKILL.md exists at skill/mesh/SKILL.md with YAML frontmatter (name, description)"
    - "Root SKILL.md delivers a full Mesh language overview (philosophy, syntax flavor, type system, ecosystem)"
    - "Root SKILL.md lists all available sub-skill commands as a flat list with one-line descriptions"
    - "Auto-load trigger instructions appear in the root SKILL.md so an AI system knows when to load it"
    - "syntax sub-skill covers functions, closures, pipes (|>), slot pipe (|N>), and operators with real code examples"
    - "types sub-skill covers all primitive types, structs, ADTs, generics, Option, and Result with real code examples"
  artifacts:
    - path: "skill/mesh/SKILL.md"
      provides: "Root skill entry point with overview and sub-topic routing"
      contains: "name: mesh"
    - path: "skill/mesh/skills/syntax/SKILL.md"
      provides: "Functions, closures, pipe operators, slot pipe tutorial"
      contains: "fn main()"
    - path: "skill/mesh/skills/types/SKILL.md"
      provides: "Type system tutorial: primitives, structs, ADTs, generics, Option/Result"
      contains: "struct"
  key_links:
    - from: "skill/mesh/SKILL.md"
      to: "skill/mesh/skills/*/SKILL.md"
      via: "sub-skill command list with paths"
      pattern: "skills/"
---

<objective>
Create the Mesh agent skill directory structure, root SKILL.md entry point, and the foundational sub-skills for syntax and types.

Purpose: Establish the skill scaffold that AI agents can auto-load to answer Mesh programming questions. The root SKILL.md is the top-level entry point — it orients an agent with language overview and routes to focused sub-skills for deep dives.

Output: skill/mesh/SKILL.md (root), skill/mesh/skills/syntax/SKILL.md, skill/mesh/skills/types/SKILL.md
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-mesh-agent-skill/121-CONTEXT.md

Reference skill format model: /Users/sn0w/.codex/skills/cadence/SKILL.md
</context>

<interfaces>
<!-- Structural model from cadence skill at /Users/sn0w/.codex/skills/cadence/SKILL.md -->
<!-- Root SKILL.md format: -->
---
name: <skill-name>
description: <one-sentence description for auto-loading trigger>
---

# <Skill Name>

## <Section>
1. Rule or guidance item.
2. Rule or guidance item.

## <Section>
...

<!-- Sub-skills live in skills/<topic>/SKILL.md inside the skill directory -->
<!-- Each sub-skill also has YAML frontmatter with name + description -->
<!-- Content style: tutorial prose + numbered rules + code examples -->
<!-- Code blocks: ```mesh fenced blocks with real runnable Mesh code -->

<!-- Real Mesh syntax examples sourced from tests/e2e/: -->

<!-- Functions (from tests/e2e/comprehensive.mpl): -->
fn add(a :: Int, b :: Int) -> Int do
  a + b
end

fn main() do
  let piped = 3 |> double
  println("#{piped}")
end

<!-- Closures (from tests/e2e/closures.mpl): -->
let n = 5
let add_n = fn(x :: Int) -> x + n end
println("#{add_n(3)}")

<!-- Slot pipe (from tests/e2e/slot_pipe_basic.mpl): -->
let result1 = 10 |2> add(1)   # desugars to add(1, 10) = 11
let result2 = "world" |2> concat3("hello ", " !")  # concat3("hello ", "world", " !")

<!-- Slot pipe chain (from tests/e2e/slot_pipe_chain.mpl): -->
# Chains: value |2> f(a) |> g()

<!-- Struct (from tests/e2e/deriving_json_basic.mpl): -->
struct User do
  name :: String
  age :: Int
  score :: Float
  active :: Bool
end deriving(Json)

let u = User { name: "Alice", age: 30, score: 95.5, active: true }

<!-- Generic struct (from tests/e2e/generic_deriving.mpl): -->
struct Box<T> do
  value :: T
end deriving(Display, Eq)

let b1 = Box { value: 42 }

<!-- ADT sum type (from tests/e2e/adts.mpl): -->
type Color do
  Red
  Green
  Blue
end

<!-- Option and Result: -->
# Result type: T!E (shorthand for Result<T, E>)
fn validate(x :: Int) -> Int!String do
  if x <= 0 do
    return Err("must be positive")
  end
  Ok(x)
end
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create skill directory and root SKILL.md</name>
  <files>skill/mesh/SKILL.md</files>
  <action>
Create the directory skill/mesh/skills/ (nested) and write skill/mesh/SKILL.md.

The root SKILL.md must have YAML frontmatter:
```yaml
---
name: mesh
description: Mesh programming language — use for any question about Mesh syntax, types, actors, ORM, HTTP/WebSocket, stdlib, or the compiler. Auto-loads for all Mesh-related questions.
---
```

Then write the root skill body with these sections (numbered rules format matching cadence model):

## Auto-Load Trigger
Rules:
1. Auto-load this skill for ANY question about the Mesh programming language.
2. For specific topics (syntax, types, actors, etc.), immediately route to the relevant sub-skill.
3. For cross-concept questions (e.g., "how do actors use the ORM"), load both relevant sub-skills and synthesize.
4. Only document what exists in the codebase — never mention planned or roadmap features.

## What is Mesh
Rules (prose in numbered format):
1. Mesh is a statically-typed, compiled programming language designed for expressive, readable concurrency.
2. It compiles to native code via LLVM and targets production backend workloads: HTTP servers, databases, distributed actor systems.
3. Design philosophy: writing concurrent programs should feel as natural and clean as writing sequential code, with supervision and fault tolerance built into the language.
4. Mesh draws from Elixir (actors, supervisors), Rust (type safety, Result/Option), and ML (pipe operator, ADTs, pattern matching).

## Language at a Glance
Rules (describe the flavor with brief inline examples):
1. Expression-oriented: most constructs evaluate to a value; `do...end` delimits blocks.
2. Type annotations use `::` (e.g., `x :: Int`); function return types use `->`.
3. String interpolation: `"hello #{name}"` (hash-brace syntax; `${}` also accepted for compatibility).
4. Heredocs: `"""..."""` for multiline strings with embedded interpolation.
5. Pipe operator `|>` chains function calls left-to-right: `3 |> double |> println`.
6. Slot pipe `|N>` routes a value to the Nth argument position: `"world" |2> concat3("hello ", " !")`.
7. Pattern matching with `case...do...end`; `_` is the wildcard.
8. Error handling with `Result` shorthand `T!E`; `?` operator propagates errors up the call stack.
9. Actors are first-class: `actor name() do receive do ... end end`, spawned with `spawn()`.

## Type System Overview
Rules:
1. Primitives: Int, Float, Bool, String, Unit.
2. Structs: product types defined with `struct Name do fields end`.
3. ADTs (sum types): `type Name do Variant1 / Variant2(T) end` — pattern-matched with `case`.
4. Generics: `struct Box<T> do value :: T end`.
5. Option<T>: represents presence/absence — `Some(x)` / `None`.
6. Result<T, E> (shorthand `T!E`): represents success/failure — `Ok(x)` / `Err(e)`.
7. Collections: List<T>, Map<K, V>, Set<T>, Range, Queue<T>.

## Ecosystem Overview
Rules:
1. Actors & Supervisors: Erlang/OTP-style lightweight actors with typed PIDs and supervision trees.
2. HTTP/WebSocket: built-in `HTTP.router()`, `HTTP.route()`, `HTTP.serve()`, `HTTP.use()` for middleware.
3. Database: Sqlite and PostgreSQL raw clients plus an ORM query builder (deriving Row, schema DSL).
4. Stdlib: List, Map, Set, Range, Queue, Iter (pipeline), String, Json, IO, Env, Regex modules.

## Available Sub-Skills
Rules (flat list with path and one-line description — each entry is a routing instruction):
1. `skills/syntax` — Functions, closures, pipe operators (|> and |N>), operators, control flow
2. `skills/types` — Primitives, structs, ADTs, generics, Option, Result
3. `skills/pattern-matching` — case/match, pattern binding, destructuring, guards
4. `skills/error-handling` — Result, Option, ? operator, chaining, error conversion
5. `skills/traits` — Interfaces, impl blocks, deriving (Json, Row, Display, Eq, Ord), associated types
6. `skills/actors` — Actor blocks, spawn, send, receive, typed PIDs, linking, preemption
7. `skills/supervisors` — Supervisor blocks, strategies, child specs, restart policies
8. `skills/collections` — List, Map, Set, Range, Queue, Iter pipelines, map/filter/reduce
9. `skills/strings` — String interpolation, heredocs, String stdlib, Env vars, Regex
10. `skills/http` — HTTP server/client, routing, middleware, WebSocket
11. `skills/database` — Sqlite, PostgreSQL, ORM query builder, schema deriving, upserts

## Routing Rules
Rules:
1. After delivering the overview, check if the user question maps to a specific sub-skill.
2. Load the matching sub-skill(s) for deep answers rather than improvising from the overview.
3. When in doubt, load `skills/syntax` first — it covers the most foundational patterns.
  </action>
  <verify>
    <automated>test -f /Users/sn0w/Documents/dev/snow/skill/mesh/SKILL.md && grep -q "name: mesh" /Users/sn0w/Documents/dev/snow/skill/mesh/SKILL.md && grep -q "skills/syntax" /Users/sn0w/Documents/dev/snow/skill/mesh/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>skill/mesh/SKILL.md exists with name: mesh frontmatter, full language overview (what is Mesh, at-a-glance, type system, ecosystem), and flat list of all 11 sub-skill commands with one-line descriptions. skills/ subdirectory exists.</done>
</task>

<task type="auto">
  <name>Task 2: Write syntax sub-skill</name>
  <files>skill/mesh/skills/syntax/SKILL.md</files>
  <action>
Create skill/mesh/skills/syntax/SKILL.md with YAML frontmatter:
```yaml
---
name: mesh-syntax
description: Mesh language syntax: functions, closures, pipe operators (|> and |N>), control flow, operators, and let bindings.
---
```

Write a tutorial-style deep-dive with numbered rules sections. Use ONLY code examples taken from the actual test files. Cover:

## Functions
Rules:
1. Functions defined with `fn name(params) -> ReturnType do ... end`.
2. Type annotations on params use `::` separator: `fn add(a :: Int, b :: Int) -> Int do`.
3. Return type optional for Unit-returning functions: `fn main() do`.
4. Last expression in a `do...end` block is implicitly returned.
5. Explicit early return with `return value`.

Code example (from tests/e2e/comprehensive.mpl):
```mesh
fn add(a :: Int, b :: Int) -> Int do
  a + b
end

fn describe_number(n :: Int) -> String do
  case n do
    0 -> "zero"
    1 -> "one"
    _ -> "other"
  end
end

fn main() do
  println("#{add(10, 20)}")
end
```

## Closures
Rules:
1. Anonymous functions: `fn(params) -> expr end` (single expression) or `fn(params) do ... end`.
2. Closures capture variables from the enclosing scope (lexical capture).
3. Type annotations on closure params are optional when types can be inferred.
4. Closures are first-class values — assignable to `let` bindings, passable as arguments.

Code example (from tests/e2e/closures.mpl and tests/e2e/iter_pipeline.mpl):
```mesh
let n = 5
let add_n = fn(x :: Int) -> x + n end
println("#{add_n(3)}")   # 8

# Short closure form for higher-order functions:
let doubled = List.map(list, fn(x) -> x * 2 end)
let filtered = List.filter(doubled, fn(x) -> x > 10 end)
```

## Pipe Operator (|>)
Rules:
1. `expr |> func` passes expr as the first argument to func.
2. Chains read left-to-right: `3 |> double |> println`.
3. Works with any function call: `list |> List.map(fn x -> x * 2 end)`.
4. Used heavily with Iter, List, and stdlib modules for readable pipelines.

Code example (from tests/e2e/iter_pipeline.mpl):
```mesh
let result = Iter.from(list)
  |> Iter.map(fn x -> x * 2 end)
  |> Iter.filter(fn x -> x > 10 end)
  |> Iter.take(3)
  |> Iter.count()
```

## Slot Pipe Operator (|N>)
Rules:
1. `expr |N> func(a, b, ...)` routes expr to argument position N (1-based, N >= 2).
2. `value |2> func(a)` desugars to `func(a, value)` — value inserted at position 2.
3. `value |2> func(a, c)` desugars to `func(a, value, c)` — value inserted at slot 2.
4. Chainable with regular pipe: `a |2> f(b) |> g()`.
5. Compiler error if slot N exceeds the function's arity.
6. Use when the natural "subject" argument is not in position 1.

Code example (from tests/e2e/slot_pipe_basic.mpl):
```mesh
fn add(a :: Int, b :: Int) -> Int do
  a + b
end

fn concat3(a :: String, b :: String, c :: String) -> String do
  a <> b <> c
end

fn main() do
  # 10 |2> add(1) = add(1, 10) = 11
  let result1 = 10 |2> add(1)
  println("#{result1}")

  # "world" |2> concat3("hello ", " !") = concat3("hello ", "world", " !")
  let result2 = "world" |2> concat3("hello ", " !")
  println(result2)
end
```

## Let Bindings
Rules:
1. `let name = expr` binds a value. Bindings are immutable by default.
2. Rebinding with the same name shadows the previous binding: `let x = 1` then `let x = x + 1`.
3. Type inference is automatic — explicit type annotation rarely needed.
4. Destructuring in let: `let Ok(v) = result` (use `case` for safe exhaustive matching).

## Control Flow
Rules:
1. `if condition do ... else ... end` — condition is a Bool expression.
2. `if` is an expression: both branches return a value of the same type.
3. `case expr do pattern -> value ... end` for pattern matching (see skills/pattern-matching).
4. `for item in collection do ... end` for iteration over lists, maps, sets, ranges.
5. `while condition do ... end` for imperative loops.
6. `break` and `continue` work inside `for` and `while`.

Code example (from tests/e2e/for_in_list.mpl and tests/e2e/while_loop.mpl):
```mesh
for item in list do
  println("#{item}")
end

let i = 0
while i < 10 do
  let i = i + 1
end
```

## Operators
Rules:
1. Arithmetic: `+`, `-`, `*`, `/`, `%`
2. Comparison: `==`, `!=`, `<`, `>`, `<=`, `>=`
3. Boolean: `and`, `or`, `not`
4. String concatenation: `<>`
5. Type cast/conversion: use `to_string()`, `to_int()`, `to_float()` methods on values.
  </action>
  <verify>
    <automated>test -f /Users/sn0w/Documents/dev/snow/skill/mesh/skills/syntax/SKILL.md && grep -q "name: mesh-syntax" /Users/sn0w/Documents/dev/snow/skill/mesh/skills/syntax/SKILL.md && grep -q "|2>" /Users/sn0w/Documents/dev/snow/skill/mesh/skills/syntax/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>skill/mesh/skills/syntax/SKILL.md exists with frontmatter, and covers functions, closures, |> pipe, |N> slot pipe (with real code from slot_pipe_basic.mpl), let bindings, control flow, and operators.</done>
</task>

<task type="auto">
  <name>Task 3: Write types sub-skill</name>
  <files>skill/mesh/skills/types/SKILL.md</files>
  <action>
Create skill/mesh/skills/types/SKILL.md with YAML frontmatter:
```yaml
---
name: mesh-types
description: Mesh type system: primitive types, structs, ADTs (sum types), generics, Option, Result, and type annotations.
---
```

Write a tutorial-style deep-dive. Use ONLY code examples from actual test files. Cover:

## Primitive Types
Rules:
1. `Int` — signed integer (maps to i64 at runtime)
2. `Float` — double-precision floating point
3. `Bool` — `true` / `false`
4. `String` — UTF-8 string (immutable)
5. `Unit` — the type of expressions with no meaningful value (implicit return from void functions)
6. Type conversion: call `.to_string()`, `.to_int()`, `.to_float()`, `.to_bool()` on primitive values.

## Structs (Product Types)
Rules:
1. Defined with `struct Name do field :: Type ... end`.
2. Instantiated with `Name { field: value, ... }` — all fields required, any order.
3. Field access via dot notation: `user.name`, `user.age`.
4. Structs can derive protocols: `end deriving(Json, Row, Display, Eq, Ord)`.
5. Generic structs: `struct Box<T> do value :: T end` — type parameter in angle brackets.

Code example (from tests/e2e/deriving_json_basic.mpl and tests/e2e/generic_deriving.mpl):
```mesh
struct User do
  name :: String
  age :: Int
  score :: Float
  active :: Bool
end deriving(Json)

let u = User { name: "Alice", age: 30, score: 95.5, active: true }
println(u.name)   # "Alice"
println("#{u.age}")

# Generic struct:
struct Box<T> do
  value :: T
end deriving(Display, Eq)

let b1 = Box { value: 42 }
let bs = Box { value: "hello" }
println("#{b1}")
```

## ADTs / Sum Types
Rules:
1. Defined with `type Name do Variant1 / Variant2(T) / Variant3(A, B) end`.
2. Nullary variants (no payload): `Red`, `Green`, `Blue`.
3. Payload variants carry typed data: `Some(T)`, `Ok(T)`, `Err(E)`.
4. Constructed by name: `let c = Red`, `let r = Ok(42)`, `let e = Err("oops")`.
5. Consumed with `case` pattern matching — compiler enforces exhaustiveness.

Code example (from tests/e2e/adts.mpl and tests/e2e/try_operator_result.mpl):
```mesh
type Color do
  Red
  Green
  Blue
end

let c = Blue
case c do
  Red -> println("red")
  Green -> println("green")
  Blue -> println("blue")
end

# Built-in Result type (T!E shorthand):
fn validate(x :: Int) -> Int!String do
  if x <= 0 do
    return Err("must be positive")
  end
  Ok(x)
end
```

## Option<T>
Rules:
1. `Option<T>` represents a value that may or may not be present.
2. Variants: `Some(value)` and `None`.
3. Unwrap safely with `case`: `case opt do Some(v) -> ... None -> ... end`.
4. `?` operator on Option: returns `None` early if the value is None.

Code example (from tests/e2e/try_option_some_path.mpl):
```mesh
fn find_first(list :: List<Int>, target :: Int) -> Option<Int> do
  List.find(list, fn(x) -> x == target end)
end

case find_first([1, 2, 3], 2) do
  Some(v) -> println("found: #{v}")
  None -> println("not found")
end
```

## Result<T, E> and the ? Operator
Rules:
1. `Result<T, E>` represents success (`Ok(T)`) or failure (`Err(E)`).
2. Shorthand type syntax: `T!E` means `Result<T, E>`.
3. `?` operator: if the expression is `Err(e)`, immediately return `Err(e)` from the current function. The function's return type must be `T!SomeError`.
4. Chaining with `?` composes fallible operations without nested case expressions.

Code example (from tests/e2e/try_operator_result.mpl):
```mesh
fn validate_positive(x :: Int) -> Int!String do
  if x <= 0 do
    return Err("must be positive")
  end
  Ok(x)
end

fn process(x :: Int) -> String!String do
  let v = validate_positive(x)?
  let w = validate_small(v)?
  Ok("valid: #{w}")
end

case process(42) do
  Ok(s) -> println(s)
  Err(e) -> println("error: #{e}")
end
```

## Collections
Rules:
1. `List<T>` — ordered, resizable list. See skills/collections for full API.
2. `Map<K, V>` — key-value store. Keys are typically String.
3. `Set<T>` — unordered unique values.
4. `Queue<T>` — FIFO queue.
5. `Range` — integer range, used with `for` loops or `Iter.from`.
6. List literal syntax: `[1, 2, 3]`.

## Type Inference
Rules:
1. Types are inferred from context — explicit annotations rarely needed in function bodies.
2. Function signatures require explicit parameter types and return type.
3. When type inference fails, add `:: Type` annotation to the `let` binding.
  </action>
  <verify>
    <automated>test -f /Users/sn0w/Documents/dev/snow/skill/mesh/skills/types/SKILL.md && grep -q "name: mesh-types" /Users/sn0w/Documents/dev/snow/skill/mesh/skills/types/SKILL.md && grep -q "ADT" /Users/sn0w/Documents/dev/snow/skill/mesh/skills/types/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>skill/mesh/skills/types/SKILL.md exists with frontmatter and covers all primitive types, structs (with field access and deriving), ADTs/sum types, Option, Result/? operator, collection types, and generics — all with real code examples from the test suite.</done>
</task>

</tasks>

<verification>
Run all verify commands:
```bash
test -f /Users/sn0w/Documents/dev/snow/skill/mesh/SKILL.md && grep -q "name: mesh" /Users/sn0w/Documents/dev/snow/skill/mesh/SKILL.md && echo "root ok"
test -f /Users/sn0w/Documents/dev/snow/skill/mesh/skills/syntax/SKILL.md && grep -q "|2>" /Users/sn0w/Documents/dev/snow/skill/mesh/skills/syntax/SKILL.md && echo "syntax ok"
test -f /Users/sn0w/Documents/dev/snow/skill/mesh/skills/types/SKILL.md && grep -q "ADT" /Users/sn0w/Documents/dev/snow/skill/mesh/skills/types/SKILL.md && echo "types ok"
```
</verification>

<success_criteria>
- skill/mesh/SKILL.md exists with name: mesh frontmatter, full language overview, and 11-item sub-skill command list
- skill/mesh/skills/syntax/SKILL.md covers functions, closures, |>, |N> with real code examples
- skill/mesh/skills/types/SKILL.md covers all primitive types, structs, ADTs, Option, Result, generics
- All code examples are sourced from actual test files (not invented)
- Directory structure: skill/mesh/SKILL.md + skill/mesh/skills/{syntax,types}/SKILL.md
</success_criteria>

<output>
After completion, create `.planning/phases/121-mesh-agent-skill/121-01-SUMMARY.md`
</output>
