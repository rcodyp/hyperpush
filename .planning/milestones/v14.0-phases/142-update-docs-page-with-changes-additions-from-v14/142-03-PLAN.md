---
phase: 142-update-docs-page-with-changes-additions-from-v14
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/skill/mesh/SKILL.md
  - tools/skill/mesh/skills/http/SKILL.md
autonomous: true
requirements:
  - HTTP-01
  - HTTP-02
  - HTTP-03
  - HTTP-04
  - HTTP-05
  - HTTP-06
  - HTTP-07
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-06
  - TEST-07
  - TEST-08
  - TEST-09
  - CRYPTO-01
  - CRYPTO-02
  - CRYPTO-03
  - CRYPTO-04
  - CRYPTO-05
  - CRYPTO-06
  - ENCODE-01
  - ENCODE-02
  - ENCODE-03
  - ENCODE-04
  - ENCODE-05
  - ENCODE-06
  - DTIME-01
  - DTIME-02
  - DTIME-03
  - DTIME-04
  - DTIME-05
  - DTIME-06
  - DTIME-07
  - DTIME-08

must_haves:
  truths:
    - "The Mesh top-level skill lists Crypto, Base64, Hex, DateTime, and Testing in the Ecosystem Overview prose"
    - "The HTTP skill has a new section documenting Http.build, Http.header, Http.body, Http.timeout, Http.send, Http.stream, Http.client, Http.send_with, Http.client_close"
    - "The HTTP skill clearly separates HTTP Server (uppercase HTTP.*) from HTTP Client v14 (lowercase Http.*)"
  artifacts:
    - path: "tools/skill/mesh/SKILL.md"
      provides: "Updated ecosystem overview with v14 stdlib and testing described in Ecosystem Overview prose"
      contains: "Testing"
    - path: "tools/skill/mesh/skills/http/SKILL.md"
      provides: "HTTP Client v14 builder API documentation"
      contains: "Http.build"
  key_links:
    - from: "tools/skill/mesh/SKILL.md"
      to: "tools/skill/mesh/skills/http/SKILL.md"
      via: "Available Sub-Skills routing entry"
      pattern: "skills/http"
---

<objective>
Update the Mesh agent skill files to reflect v14.0 additions: expand the ecosystem overview in the top-level SKILL.md to list the new stdlib modules and add a testing sub-skill entry, and add a comprehensive HTTP Client v14 section to the HTTP skill.

Purpose: Claude uses these skill files as the authoritative source when answering questions about Mesh. Without this update, Claude will give stale or incorrect information about HTTP client API and will have no knowledge of the Crypto/Encoding/DateTime/Testing APIs.
Output: Updated tools/skill/mesh/SKILL.md and tools/skill/mesh/skills/http/SKILL.md.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-update-docs-page-with-changes-additions-from-v14/142-RESEARCH.md

<interfaces>
<!-- Current state of skill files. Executor edits ONLY the listed sections. -->

tools/skill/mesh/SKILL.md — current "Ecosystem Overview" section (lines 40-45):
```markdown
## Ecosystem Overview
1. Actors & Supervisors: Erlang/OTP-style lightweight actors with typed PIDs and supervision trees.
2. HTTP/WebSocket: built-in `HTTP.router()`, `HTTP.route()`, `HTTP.serve()`, `HTTP.use()` for middleware.
3. Database: Sqlite and PostgreSQL raw clients plus an ORM query builder (deriving Row, schema DSL).
4. Stdlib: List, Map, Set, Range, Queue, Iter (pipeline), String, Json (encode/parse + `json { }` literals), IO, Env, Regex modules.
5. Concurrency Utilities: Job module for async task spawning/awaiting; service blocks for stateful OTP-style gen_server processes.
```

tools/skill/mesh/SKILL.md — current "Available Sub-Skills" section (lines 47-58):
```markdown
## Available Sub-Skills
1. `skills/syntax` — Functions, closures, pipe operators (|> and |N>), operators, control flow
2. `skills/types` — Primitives, structs, ADTs, generics, Option, Result
3. `skills/pattern-matching` — case/match, pattern binding, destructuring, guards
4. `skills/error-handling` — Result, Option, ? operator, chaining, error conversion
5. `skills/traits` — Interfaces, impl blocks, deriving (Json, Row, Display, Eq, Ord), associated types
6. `skills/actors` — Actor blocks, spawn, send, receive, typed PIDs, services (call/cast), Job.async/await
7. `skills/supervisors` — Supervisor blocks, strategies, child specs, restart policies
8. `skills/collections` — List, Map, Set, Range, Queue, Iter pipelines, map/filter/reduce
9. `skills/strings` — String interpolation, heredocs, `json { }` object literals, String stdlib, Env vars, Regex
10. `skills/http` — HTTP server/client, routing, middleware, WebSocket
11. `skills/database` — Sqlite, PostgreSQL, ORM query builder, schema deriving, upserts
```

tools/skill/mesh/skills/http/SKILL.md — current "## HTTP Client" section (lines 101-115):
```markdown
## HTTP Client

Rules:
1. `HTTP.get(url) -> Result<String, String>` — synchronous GET; returns body or error message.
2. Returns `Ok(body_string)` on 2xx, `Err(message)` on failure.
3. Use the `?` operator or `case` to handle the result.

Code example (from tests/e2e/stdlib_http_client.mpl):
```mesh
let result = HTTP.get("http://api.example.com/data")
case result do
  Ok(body) -> println(body)
  Err(msg) -> println("request failed: #{msg}")
end
```
```

CRITICAL: Http (client) vs HTTP (server) distinction must be maintained throughout. Do not rename existing HTTP.* server rules. Do not rename existing HTTP.get rule — leave it and add new Http.* rules.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update top-level SKILL.md — ecosystem overview + sub-skills</name>
  <files>tools/skill/mesh/SKILL.md</files>
  <action>
    Edit `tools/skill/mesh/SKILL.md`. Make two targeted changes:

    **Change 1: Update the Ecosystem Overview section.**

    Replace the current item 2 (HTTP/WebSocket) and item 4 (Stdlib) lines with expanded versions, and add an item 6 for the new HTTP Client and Testing:

    Change:
    ```markdown
    ## Ecosystem Overview
    1. Actors & Supervisors: Erlang/OTP-style lightweight actors with typed PIDs and supervision trees.
    2. HTTP/WebSocket: built-in `HTTP.router()`, `HTTP.route()`, `HTTP.serve()`, `HTTP.use()` for middleware.
    3. Database: Sqlite and PostgreSQL raw clients plus an ORM query builder (deriving Row, schema DSL).
    4. Stdlib: List, Map, Set, Range, Queue, Iter (pipeline), String, Json (encode/parse + `json { }` literals), IO, Env, Regex modules.
    5. Concurrency Utilities: Job module for async task spawning/awaiting; service blocks for stateful OTP-style gen_server processes.
    ```

    To:
    ```markdown
    ## Ecosystem Overview
    1. Actors & Supervisors: Erlang/OTP-style lightweight actors with typed PIDs and supervision trees.
    2. HTTP Server/WebSocket: built-in `HTTP.router()`, `HTTP.route()`, `HTTP.serve()`, `HTTP.use()` for middleware; `Ws.serve()` for WebSocket.
    3. Database: Sqlite and PostgreSQL raw clients plus an ORM query builder (deriving Row, schema DSL).
    4. Stdlib: List, Map, Set, Range, Queue, Iter (pipeline), String, Json (encode/parse + `json { }` literals), IO, Env, Regex, Crypto (sha256/sha512/hmac/uuid4), Base64, Hex, DateTime modules.
    5. Concurrency Utilities: Job module for async task spawning/awaiting; service blocks for stateful OTP-style gen_server processes.
    6. HTTP Client (v14): fluent builder API — `Http.build`, `Http.header`, `Http.body`, `Http.timeout`, `Http.send`, `Http.stream`, `Http.client`, `Http.send_with` (note: lowercase `Http`, distinct from `HTTP` server).
    7. Testing: `meshc test` runner, `test()/describe()/setup/teardown` DSL, `assert/assert_eq/assert_ne/assert_raises`, `Test.mock_actor`, `assert_receive`.
    8. Package Registry: `meshpkg` CLI for publish/install/search/login; `mesh.toml` manifest with `[package]` and `[dependencies]` sections.
    ```

    No other changes to SKILL.md. Do NOT add a `skills/testing` entry to the Available Sub-Skills routing table — no `tools/skill/mesh/skills/testing/SKILL.md` file exists or is being created in this phase. Testing knowledge is captured only in the Ecosystem Overview prose (item 7 above).
  </action>
  <verify>
    <automated>grep -q "Crypto" /Users/sn0w/Documents/dev/mesh/tools/skill/mesh/SKILL.md && grep -q "DateTime" /Users/sn0w/Documents/dev/mesh/tools/skill/mesh/SKILL.md && grep -q "Http\.build" /Users/sn0w/Documents/dev/mesh/tools/skill/mesh/SKILL.md && grep -q "meshc test" /Users/sn0w/Documents/dev/mesh/tools/skill/mesh/SKILL.md && echo "SKILL.md OK"</automated>
  </verify>
  <done>SKILL.md Ecosystem Overview mentions Crypto, Base64, Hex, DateTime; mentions Http.build for HTTP Client; mentions meshc test for Testing; the Available Sub-Skills list is NOT given a skills/testing entry (no such file exists)</done>
</task>

<task type="auto">
  <name>Task 2: Add HTTP Client v14 section to http skill</name>
  <files>tools/skill/mesh/skills/http/SKILL.md</files>
  <action>
    Edit `tools/skill/mesh/skills/http/SKILL.md`. Add a new "## HTTP Client v14 (Builder API)" section immediately AFTER the existing "## HTTP Client" section (which documents `HTTP.get`). Do NOT rename or remove the existing `## HTTP Client` section — it remains as the legacy single-call documentation.

    Insert the following block after the existing HTTP Client section's closing code block:

    ```markdown
    ## HTTP Client v14 (Builder API)

    Rules:
    1. `Http.build(method, url)` — creates a new request. `method` is an atom: `:get`, `:post`, `:put`, `:delete`. Returns a Request handle.
    2. `Http.header(req, key, value)` — adds a header. Returns updated request (rebind).
    3. `Http.body(req, s)` — sets the request body string (for POST/PUT). Returns updated request.
    4. `Http.timeout(req, ms)` — sets a per-request timeout in milliseconds. Returns updated request.
    5. `Http.send(req)` — executes the request. Returns `Result<String, String>` — `Ok(body)` on 2xx, `Err(message)` otherwise.
    6. `Http.stream(req, fn chunk -> ... end)` — streams response body chunk by chunk in an OS thread. Callback returns `"ok"` to continue, `"stop"` to cancel. Does not buffer the full body.
    7. `Http.client()` — creates a keep-alive HTTP client handle for connection reuse.
    8. `Http.send_with(client, req)` — sends request using the client's connection pool. Returns `Result<String, String>`.
    9. `Http.client_close(client)` — releases the client and its connections.
    10. CRITICAL: `Http.*` (lowercase) = HTTP CLIENT. `HTTP.*` (uppercase) = HTTP SERVER. Never mix them.

    Code example — GET with headers (from tests/e2e/http_client_builder.mpl):
    ```mesh
    fn main() do
      let req = Http.build(:get, "https://api.example.com/data")
      let req = Http.header(req, "Authorization", "Bearer token")
      let req = Http.timeout(req, 5000)
      let result = Http.send(req)
      case result do
        Ok(resp) -> println(resp)
        Err(e) -> println("error: #{e}")
      end
    end
    ```

    Code example — POST with body (from tests/e2e/http_client_builder.mpl):
    ```mesh
    fn main() do
      let req = Http.build(:post, "https://api.example.com/items")
      let req = Http.header(req, "Content-Type", "application/json")
      let req = Http.body(req, json { name: "widget" })
      let result = Http.send(req)
      case result do
        Ok(resp) -> println(resp)
        Err(e) -> println(e)
      end
    end
    ```

    Code example — Streaming (from tests/e2e/http_stream_compile.mpl):
    ```mesh
    fn main() do
      let req = Http.build(:get, "https://example.com/stream")
      let _handle = Http.stream(req, fn chunk do
        println(chunk)
        "ok"
      end)
    end
    ```

    Code example — Keep-alive client (from tests/e2e/http_client_keepalive.mpl):
    ```mesh
    fn main() do
      let client = Http.client()
      let req = Http.build(:get, "https://api.example.com/data")
      let result = Http.send_with(client, req)
      case result do
        Ok(resp) -> println(resp)
        Err(e) -> println(e)
      end
      Http.client_close(client)
    end
    ```
    ```

    After this insertion, also update the description in the frontmatter `description` field to mention the v14 builder. Change:
    ```yaml
    description: Mesh HTTP: server routing (HTTP.router/route/serve), middleware (HTTP.use), path parameters, HTTP client (HTTP.get), response helpers, and WebSocket.
    ```
    To:
    ```yaml
    description: Mesh HTTP: server routing (HTTP.router/route/serve), middleware (HTTP.use), path parameters, HTTP client v14 fluent builder (Http.build/send/stream), legacy HTTP.get, and WebSocket.
    ```
  </action>
  <verify>
    <automated>grep -q "Http\.build" /Users/sn0w/Documents/dev/mesh/tools/skill/mesh/skills/http/SKILL.md && grep -q "Http\.stream" /Users/sn0w/Documents/dev/mesh/tools/skill/mesh/skills/http/SKILL.md && grep -q "Http\.client" /Users/sn0w/Documents/dev/mesh/tools/skill/mesh/skills/http/SKILL.md && grep -q "HTTP Client v14" /Users/sn0w/Documents/dev/mesh/tools/skill/mesh/skills/http/SKILL.md && echo "http SKILL.md OK"</automated>
  </verify>
  <done>tools/skill/mesh/skills/http/SKILL.md has a "## HTTP Client v14 (Builder API)" section with all 9 rules and working code examples for GET with headers, POST with body, streaming, and keep-alive; existing HTTP server and HTTP.get sections are unchanged</done>
</task>

</tasks>

<verification>
1. `grep "Crypto" tools/skill/mesh/SKILL.md` — in Ecosystem Overview
2. `grep "DateTime" tools/skill/mesh/SKILL.md` — in Ecosystem Overview
3. `grep "meshc test" tools/skill/mesh/SKILL.md` — in Ecosystem Overview (Testing item)
4. `grep "Http\.build" tools/skill/mesh/SKILL.md` — in Ecosystem Overview item 6
5. `grep "skills/testing" tools/skill/mesh/SKILL.md` — must NOT be present (no such skill file exists)
6. `grep "HTTP Client v14" tools/skill/mesh/skills/http/SKILL.md` — new section heading present
7. `grep "Http\.build" tools/skill/mesh/skills/http/SKILL.md` — rule 1 present
8. `grep "Http\.stream" tools/skill/mesh/skills/http/SKILL.md` — streaming rule present
9. `grep "HTTP\.router" tools/skill/mesh/skills/http/SKILL.md` — existing server section preserved
10. `grep "HTTP\.get" tools/skill/mesh/skills/http/SKILL.md` — legacy client section preserved
</verification>

<success_criteria>
- tools/skill/mesh/SKILL.md Ecosystem Overview lists Crypto, Base64, Hex, DateTime in stdlib; has item 6 for Http v14 client; has item 7 for Testing; has item 8 for Package Registry
- tools/skill/mesh/SKILL.md Available Sub-Skills does NOT include a `skills/testing` entry (no testing sub-skill file exists; testing is documented only in the Ecosystem Overview prose)
- tools/skill/mesh/skills/http/SKILL.md has a new "HTTP Client v14 (Builder API)" section with all functions (build, header, body, timeout, send, stream, client, send_with, client_close) documented with code examples
- All existing HTTP server content in both skill files is untouched
- The Http vs HTTP naming distinction is explicitly noted in the rules
</success_criteria>

<output>
After completion, create `.planning/phases/142-update-docs-page-with-changes-additions-from-v14/142-03-SUMMARY.md`
</output>
