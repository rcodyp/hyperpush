---
phase: 145-packages-redesign
plan: "02"
type: execute
wave: 2
depends_on: ["145-01"]
files_modified:
  - packages-website/src/routes/+layout.svelte
  - packages-website/src/routes/+page.svelte
autonomous: true
requirements: []

must_haves:
  truths:
    - "The layout renders a sticky navbar with backdrop blur, Mesh Packages logo, and inline search form"
    - "The home page renders a hero section with title, subtitle, and total package count"
    - "Packages display in a responsive 3-column grid (1 col mobile, 2 col md, 3 col lg) of card components"
    - "Each card shows package name (linked), version badge in JetBrains Mono, description, and download count"
    - "Cards use `pkg.version` (correct field from list API) not `pkg.latest_version`"
    - "Empty state shows a helpful message with publish CTA"
    - "Dark mode toggle in navbar persists preference to localStorage and toggles .dark class on html element"
    - "A footer links to meshlang.dev and GitHub"
  artifacts:
    - path: "packages-website/src/routes/+layout.svelte"
      provides: "Sticky navbar with search + dark mode toggle, footer, app.css import"
      min_lines: 60
    - path: "packages-website/src/routes/+page.svelte"
      provides: "Hero section + responsive package grid with card components"
      min_lines: 80
  key_links:
    - from: "packages-website/src/routes/+layout.svelte"
      to: "packages-website/src/app.css"
      via: "import '../app.css' at top of script block"
      pattern: "import.*app.css"
    - from: "packages-website/src/routes/+page.svelte"
      to: "pkg.version"
      via: "correct API field name from list endpoint"
      pattern: "pkg\\.version"
---

<objective>
Redesign the global layout component and home/browse page of the packages website to match the landing page's production-grade visual design.

Purpose: The layout wraps all pages — getting it right ensures visual consistency everywhere. The home page is the most visited page and must make a strong first impression with the hero + package grid.

Output: A sticky navbar with backdrop blur, logo, search, and dark mode toggle; a footer with links; a home page with hero section and responsive card grid.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-completely-redesign-the-mesh-packages-page-it-is-extremely-bare-and-simple-it-should-be-production-grade-and-look-good-and-fit-the-theme-of-the-landing-page/145-RESEARCH.md

<interfaces>
<!-- Tailwind tokens available after Plan 01 — use these utility classes -->
<!-- bg-background, text-foreground, bg-card, text-card-foreground -->
<!-- bg-muted, text-muted-foreground, border-border -->
<!-- bg-primary, text-primary-foreground -->
<!-- font-sans (Inter), font-mono (JetBrains Mono) -->
<!-- .reveal, .reveal.is-visible, .reveal-delay-1 through .reveal-delay-4 -->

<!-- List API response shape — confirmed from registry/src/routes/search.rs -->
<!-- data.packages: Array<{ name: string, version: string, description: string }> -->
<!-- NOTE: field is `version` NOT `latest_version` -->
<!-- Also available (from packages table): download_count, updated_at — but list API only returns name/version/description -->

<!-- Home page current server loader (packages-website/src/routes/+page.server.js) — DO NOT MODIFY -->
<!-- Returns: { packages: Array<{name, version, description}>, error?: string } -->

<!-- Current layout (replace entirely): -->
<!-- <nav style="padding:1rem;border-bottom:1px solid #eee;font-family:sans-serif"> -->
<!--   <a href="/">Mesh Packages</a> -->
<!--   <form action="/search" method="GET"> -->
<!--     <input name="q" placeholder="Search packages..."/> -->
<!--   </form> -->
<!-- </nav> -->
<!-- <main style="max-width:960px;margin:2rem auto"> -->
<!--   <slot /> -->
<!-- </main> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign +layout.svelte with sticky navbar, dark mode toggle, and footer</name>
  <files>
    packages-website/src/routes/+layout.svelte
  </files>
  <action>
    Replace the entire contents of `packages-website/src/routes/+layout.svelte` with a production-grade layout. This is a complete rewrite — delete the old inline-styled nav.

    The layout must:
    1. Import `'../app.css'` (the Tailwind + OKLCH file from Plan 01)
    2. Import `{ Package, Search, Sun, Moon }` from `'lucide-svelte'`
    3. Import `{ onMount }` from `'svelte'`
    4. Use `export let data;` for page data

    Dark mode toggle logic (in `<script>`):
    ```js
    let dark = false;
    onMount(() => {
      dark = document.documentElement.classList.contains('dark');
    });
    function toggleDark() {
      dark = !dark;
      document.documentElement.classList.toggle('dark', dark);
      localStorage.setItem('theme', dark ? 'dark' : 'light');
    }
    ```

    Header structure:
    ```html
    <header class="sticky top-0 z-50 w-full border-b border-border/50 bg-background/80 backdrop-blur-xl">
      <div class="mx-auto flex h-14 max-w-6xl items-center gap-4 px-4 lg:px-6">
        <!-- Logo -->
        <a href="/" class="flex items-center gap-2 shrink-0 no-underline">
          <Package class="size-5 text-foreground" />
          <span class="text-sm font-semibold text-foreground">Mesh Packages</span>
        </a>

        <!-- Search form (right side) -->
        <form action="/search" method="GET" class="flex items-center ml-auto gap-3">
          <div class="relative hidden sm:block">
            <Search class="absolute left-3 top-1/2 -translate-y-1/2 size-3.5 text-muted-foreground pointer-events-none" />
            <input
              name="q"
              placeholder="Search packages..."
              class="h-9 w-48 rounded-md border border-border bg-muted pl-9 pr-3 text-sm text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-foreground/30 md:w-64 transition-colors"
            />
          </div>
          <!-- Dark mode toggle button -->
          <button
            type="button"
            on:click={toggleDark}
            class="flex h-9 w-9 items-center justify-center rounded-md border border-border bg-muted text-muted-foreground transition-colors hover:bg-card hover:text-foreground"
            aria-label="Toggle dark mode"
          >
            {#if dark}
              <Sun class="size-4" />
            {:else}
              <Moon class="size-4" />
            {/if}
          </button>
          <!-- Docs link -->
          <a
            href="https://meshlang.dev"
            target="_blank"
            class="hidden text-sm text-muted-foreground transition-colors hover:text-foreground md:block no-underline"
          >Docs</a>
        </form>
      </div>
    </header>
    ```

    Main content area:
    ```html
    <main class="min-h-screen bg-background">
      <slot />
    </main>
    ```

    Footer:
    ```html
    <footer class="border-t border-border py-10">
      <div class="mx-auto max-w-6xl px-4 text-center text-sm text-muted-foreground">
        <p>
          <a href="https://meshlang.dev" class="transition-colors hover:text-foreground">meshlang.dev</a>
          &nbsp;&middot;&nbsp;
          <a href="https://github.com/snowdamiz/mesh-lang" class="transition-colors hover:text-foreground">GitHub</a>
          &nbsp;&middot;&nbsp;
          <a href="https://meshlang.dev/docs/tooling" class="transition-colors hover:text-foreground">meshpkg docs</a>
        </p>
        <p class="mt-2 text-xs">The Mesh package registry. Publish with <code class="font-mono">meshpkg publish</code>.</p>
      </div>
    </footer>
    ```
  </action>
  <verify>
    <automated>grep -c "app.css" /Users/sn0w/Documents/dev/mesh/packages-website/src/routes/+layout.svelte && grep -c "backdrop-blur" /Users/sn0w/Documents/dev/mesh/packages-website/src/routes/+layout.svelte && grep -c "toggleDark" /Users/sn0w/Documents/dev/mesh/packages-website/src/routes/+layout.svelte && echo "LAYOUT_OK"</automated>
  </verify>
  <done>+layout.svelte imports app.css; has sticky header with backdrop-blur and border; has dark mode toggle wired to localStorage + document.documentElement; has footer with meshlang.dev and GitHub links; no inline style="" attributes</done>
</task>

<task type="auto">
  <name>Task 2: Redesign +page.svelte with hero section and responsive package grid</name>
  <files>
    packages-website/src/routes/+page.svelte
  </files>
  <action>
    Replace the entire contents of `packages-website/src/routes/+page.svelte` with a production-grade home page. Complete rewrite — delete all inline-styled divs.

    Script block:
    ```js
    import { onMount } from 'svelte';
    import { Download, Package } from 'lucide-svelte';
    export let data;

    let cards = [];
    onMount(() => {
      const observer = new IntersectionObserver(
        (entries) => entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('is-visible'); }),
        { threshold: 0.08 }
      );
      cards.forEach(el => { if (el) observer.observe(el); });
      return () => observer.disconnect();
    });
    ```

    Page structure:

    **Hero section** (centered, padded, mirroring landing page section header pattern):
    ```html
    <section class="border-b border-border bg-background py-16 sm:py-20">
      <div class="mx-auto max-w-6xl px-4 text-center">
        <div class="text-xs font-mono uppercase tracking-widest text-muted-foreground">Registry</div>
        <h1 class="mt-3 text-4xl font-bold tracking-tight text-foreground sm:text-5xl">
          Mesh Packages
        </h1>
        <p class="mx-auto mt-4 max-w-lg text-lg text-muted-foreground">
          Community packages for the Mesh programming language.
        </p>
        {#if !data.error && data.packages.length > 0}
          <p class="mt-2 text-sm text-muted-foreground font-mono">
            {data.packages.length} package{data.packages.length === 1 ? '' : 's'} published
          </p>
        {/if}
        <!-- Hero search form for mobile (nav search hidden on small screens) -->
        <form action="/search" method="GET" class="mt-8 flex items-center justify-center gap-2 sm:hidden">
          <input
            name="q"
            placeholder="Search packages..."
            class="h-10 w-full max-w-xs rounded-md border border-border bg-muted px-4 text-sm text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-foreground/30"
          />
          <button type="submit" class="h-10 rounded-md bg-foreground px-4 text-sm font-medium text-primary-foreground shrink-0">
            Search
          </button>
        </form>
      </div>
    </section>
    ```

    **Package grid section**:
    ```html
    <section class="py-12 sm:py-16">
      <div class="mx-auto max-w-6xl px-4">
        {#if data.error}
          <div class="rounded-xl border border-border bg-card p-8 text-center">
            <p class="text-muted-foreground">{data.error}</p>
          </div>
        {:else if data.packages.length === 0}
          <div class="rounded-xl border border-border bg-card p-12 text-center">
            <Package class="mx-auto size-10 text-muted-foreground mb-4" />
            <h2 class="text-lg font-semibold text-foreground">No packages yet</h2>
            <p class="mt-2 text-sm text-muted-foreground">Be the first to publish a Mesh package.</p>
            <a href="https://meshlang.dev/docs/tooling" class="mt-4 inline-block rounded-md bg-foreground px-4 py-2 text-sm font-medium text-primary-foreground no-underline">
              Learn meshpkg
            </a>
          </div>
        {:else}
          <div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
            {#each data.packages as pkg, i}
              <a
                href="/packages/{pkg.name}"
                bind:this={cards[i]}
                class="reveal reveal-delay-{Math.min(i % 3 + 1, 4)} block rounded-xl border border-foreground/10 bg-card p-6 transition-all duration-300 hover:-translate-y-0.5 hover:border-foreground/30 hover:shadow-lg no-underline"
              >
                <div class="flex items-start justify-between gap-2">
                  <span class="text-base font-bold text-foreground leading-tight">{pkg.name}</span>
                  <span class="shrink-0 rounded-md bg-muted px-2 py-0.5 font-mono text-xs text-muted-foreground whitespace-nowrap">
                    v{pkg.version}
                  </span>
                </div>
                <p class="mt-2 text-sm leading-relaxed text-muted-foreground line-clamp-2">
                  {pkg.description || 'No description provided.'}
                </p>
              </a>
            {/each}
          </div>
        {/if}
      </div>
    </section>
    ```

    Key correctness requirements:
    - Use `pkg.version` NOT `pkg.latest_version` (list API returns `version`)
    - Card hover: `-translate-y-0.5`, `border-foreground/30`, `shadow-lg` (matches landing page WhyMesh cards)
    - Grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` with `gap-4`
    - No inline `style=""` attributes anywhere
    - `bind:this={cards[i]}` for IntersectionObserver reveal effect
  </action>
  <verify>
    <automated>grep -c "pkg.version" /Users/sn0w/Documents/dev/mesh/packages-website/src/routes/+page.svelte && grep -c "grid-cols" /Users/sn0w/Documents/dev/mesh/packages-website/src/routes/+page.svelte && grep -c "hover:-translate-y" /Users/sn0w/Documents/dev/mesh/packages-website/src/routes/+page.svelte && ! grep -c "style=\"" /Users/sn0w/Documents/dev/mesh/packages-website/src/routes/+page.svelte && echo "HOME_OK"</automated>
  </verify>
  <done>+page.svelte has hero section with mono label + h1 + subtitle; package grid with grid-cols-1/sm:grid-cols-2/lg:grid-cols-3; cards use pkg.version (correct); card hover animation with -translate-y-0.5 + shadow; empty state with CTA; IntersectionObserver reveal; no inline style attributes</done>
</task>

</tasks>

<verification>
Run `npm run build` in packages-website/ — must succeed with no Svelte compiler errors.
Confirm no `style="` appears in either modified file (all styling via Tailwind classes).
Confirm `pkg.version` (not `pkg.latest_version`) appears in +page.svelte.
Confirm `app.css` is imported in +layout.svelte.
</verification>

<success_criteria>
- `+layout.svelte` imports app.css; has sticky header with `sticky top-0 z-50 backdrop-blur-xl`; has dark mode toggle; has footer with meshlang.dev + GitHub links; zero inline style attributes
- `+page.svelte` has hero section + responsive 3-column grid; uses `pkg.version`; cards have hover animations matching landing page; IntersectionObserver reveal; zero inline style attributes
- `npm run build` passes in packages-website/
</success_criteria>

<output>
After completion, create `.planning/phases/145-completely-redesign-the-mesh-packages-page-it-is-extremely-bare-and-simple-it-should-be-production-grade-and-look-good-and-fit-the-theme-of-the-landing-page/145-02-SUMMARY.md`
</output>
