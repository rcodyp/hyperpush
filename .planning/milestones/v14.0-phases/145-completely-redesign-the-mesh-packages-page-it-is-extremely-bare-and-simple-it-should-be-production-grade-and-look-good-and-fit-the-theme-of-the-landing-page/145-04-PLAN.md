---
phase: 145-packages-redesign
plan: "04"
type: execute
wave: 3
depends_on: ["145-02", "145-03"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Site loads without JavaScript errors in browser console"
    - "Visual design matches landing page (Inter font, OKLCH colors, card style, navbar blur)"
    - "Dark mode toggle works and persists on refresh"
    - "Package cards navigate to detail pages"
    - "README renders as styled prose (not raw markdown text)"
    - "Install copy button works (copied to clipboard)"
    - "Registry is deployed with the new versions endpoint"
  artifacts:
    - path: "packages-website/"
      provides: "Production-ready redesigned packages site"
  key_links:
    - from: "Human visual inspection"
      to: "Design match with meshlang.dev landing page"
      via: "Side-by-side comparison"
      pattern: "n/a"
---

<objective>
Human visual verification of the complete redesign before deploying to production.

Purpose: UI quality cannot be verified by automated tests alone — the human must confirm the visual design matches the landing page, interactions work, and the site looks production-grade. This is also the gate before deploying the registry API change to production.

Output: Confirmed approval to deploy, or list of specific issues to address.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and automated smoke tests</name>
  <files></files>
  <action>
    Run the following checks and report results:

    1. Build verification:
    ```bash
    cd /Users/sn0w/Documents/dev/mesh/packages-website && npm run build 2>&1 | tail -20
    ```
    Confirm: exits 0, no Svelte compiler errors, CSS output generated.

    2. Registry cargo check:
    ```bash
    cd /Users/sn0w/Documents/dev/mesh/registry && cargo check --quiet
    ```
    Confirm: exits 0.

    3. Verify no inline styles remain in Svelte files:
    ```bash
    grep -rn 'style="' /Users/sn0w/Documents/dev/mesh/packages-website/src/routes/
    ```
    Expected: zero matches.

    4. Start dev server in background and smoke test:
    ```bash
    cd /Users/sn0w/Documents/dev/mesh/packages-website && npm run dev -- --port 5173 &
    sleep 4
    curl -s -o /dev/null -w "%{http_code}" http://localhost:5173/
    ```
    Expected: 200.

    Report all results clearly before the human verification checkpoint.
  </action>
  <verify>
    <automated>cd /Users/sn0w/Documents/dev/mesh/packages-website && npm run build 2>&1 | tail -5 && echo "BUILD_OK"</automated>
  </verify>
  <done>npm run build exits 0; cargo check exits 0; no inline style= attributes in route files</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of complete redesign</name>
  <files></files>
  <action>
    Present the verification checklist to the user. The dev server should already be running on port 5173 from Task 1.
  </action>
  <verify>
    <automated>echo "Human verification required — cannot automate visual design check"</automated>
  </verify>
  <done>Human types "approved" confirming visual design matches landing page and all interactions work correctly</done>
  <what-built>
    Complete packages-website redesign:
    - Tailwind CSS v4 + OKLCH design system matching landing page (Inter font, JetBrains Mono, monochrome tokens)
    - Sticky navbar with backdrop blur, logo, inline search, dark mode toggle
    - Home page: hero section + responsive 3-column package grid with hover animations
    - Package detail: rendered README prose + install copy button + metadata sidebar + version history
    - Search results: styled card grid matching home page
    - API bugs fixed: correct field names, correct query params, new versions endpoint in registry
  </what-built>
  <how-to-verify>
    Open http://localhost:5173/ in browser and check:

    1. Home page
       - Inter font visible (not browser default sans-serif)
       - Monochrome OKLCH palette (black/white/grey, no blue tones from browser defaults)
       - Sticky nav with backdrop blur effect on scroll
       - Package cards with subtle border and hover lift animation (-translate-y)
       - "Mesh Packages" logo with Package icon in nav

    2. Dark mode toggle (moon/sun icon in nav)
       - Page switches to dark mode (dark background, light text)
       - Refresh page — dark mode persists (localStorage)

    3. Click any package card -> navigates to /packages/{name}
       - Install command block with $ prefix and copy button
       - README renders as styled prose (headers, paragraphs, code blocks styled — NOT raw markdown text)
       - Metadata sidebar shows version, download count, owner

    4. Copy button on install command
       - Icon switches from Copy to Check for 2 seconds

    5. Search input -> submit
       - Navigates to /search?q=... with styled results grid

    6. Navigate to /packages/nonexistent-xyz-123
       - Styled 404 page with "Browse all packages" link

    7. Compare side-by-side with https://meshlang.dev
       - Same Inter font, same monochrome OKLCH palette, same card style patterns
  </how-to-verify>
  <resume-signal>Type "approved" if the redesign looks production-grade and matches the landing page design system. Or describe specific visual issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Human visual approval is the final gate. All automated checks in Task 1 must pass before the checkpoint is reached.
</verification>

<success_criteria>
- `npm run build` in packages-website/ exits 0
- `cargo check` in registry/ exits 0
- No `style=""` attributes remain in packages-website/src/routes/
- Human confirms: visual design matches landing page, dark mode works, README renders as prose, copy button works
- Human types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/145-completely-redesign-the-mesh-packages-page-it-is-extremely-bare-and-simple-it-should-be-production-grade-and-look-good-and-fit-the-theme-of-the-landing-page/145-04-SUMMARY.md`
</output>
